


<h1>New Book</h1>


<%= form_for(@book) do |f| %>
	
	<% if @book.errors.any? %>
		<p>エラーでーす</p>>
	<% end %>
	
	<h3>title</h3>
	<%= f.text_field :title, :class => "book_title" %>

	<h3> body</h3>
	<%= f.text_area :body, :class => "book_body" %>

	<%= f.submit 'Create Book' %>

<% end %>
